name: Deploy
on:
  workflow_dispatch:

  release:
    types: [published]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Run main.ts
        run: deno run --allow-env --allow-read --allow-write  --allow-net .github/workflows/merge/main.ts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
